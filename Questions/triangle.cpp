#include <iostream>
#include <conio.h>

using namespace std;

int main()
{
    float base, height;
    cout << "Enter Base of triangle : ";
    cin >> base;
    cout << "Enter height of triangle : ";
    cin >> height;
    area = (base * height) / 2;
    cout << "Area of triangle : " << area << endl;
    getch();
    return 0;
}